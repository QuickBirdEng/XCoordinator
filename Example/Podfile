use_frameworks!
platform :ios, '8.0'

target 'XCoordinator_Example' do
  pod 'XCoordinator', :path => '../'
  pod 'XCoordinator/RxSwift', :path => '../'
  pod 'Action', '~> 3.9', :inhibit_warnings => true
  pod 'RxSwift', '~> 4.5', :inhibit_warnings => true
  pod 'RxCocoa', '~> 4.5', :inhibit_warnings => true
  pod 'SwiftLint'

  target 'XCoordinator_Tests' do
    inherit! :search_paths
  end
end
